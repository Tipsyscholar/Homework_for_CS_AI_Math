# ----------------------------------------------------
# 1. 编译和构建输出 (Build/Compilation Outputs)
# ----------------------------------------------------
/dist/       # 忽略所有 'dist' 目录 (通常是构建输出)
/build/      # 忽略所有 'build' 目录
*.o          # 忽略所有目标文件
*.exe        # 忽略所有可执行文件
**/*.pyc
__pycache__/
note.ipynb
# ----------------------------------------------------
# 2. 依赖项目录 (Dependency Directories)
# ----------------------------------------------------
/node_modules    # Node.js 依赖
/vendor          # PHP/Composer 依赖
.venv/           # Python 虚拟环境

# ----------------------------------------------------
# 3. 日志、缓存和临时文件 (Logs, Cache, and Temp Files)
# ----------------------------------------------------
*.log
tmp/
.cache/

# ----------------------------------------------------
# 4. IDE 和编辑器配置 (IDE/Editor Configuration)
# ----------------------------------------------------
.idea/       # JetBrains IDEs (如 IntelliJ, PyCharm)
.vscode/     # VS Code 配置
*.swp        # Vim 临时文件

# ----------------------------------------------------
# 5. 敏感信息和本地配置 (Sensitive/Local Configuration)
# ----------------------------------------------------
# 不要提交包含密码、API 密钥等信息的本地配置文件
config.local.json
.env